export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <title>Financial Fraud Detection</title>
      </head>
      <body>{children}</body>
    </html>
  );
}
